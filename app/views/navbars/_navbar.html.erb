<nav class="navbar navbar-expand-sm navbar-dark" style="background: #154D23;">
  <a class="navbar-brand" href="<%= root_path %>">STUV</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <div class="btn-group">
          <% if logged_in?%>
            <%= link_to "Mi perfil", perfil_path, class: "btn text-light", style: "background: #958419;" %>
            <%= link_to "Cerrar sesión", logout_path, method: :delete, class: "btn btn-danger" %>
          <% else %>
            <a href="<%= new_user_path %>" class="btn text-light" style="background: #958419;">Crear cuenta</a>
          <% end %>
        </div>
      </li>
    </ul>
  </div>
</nav>